# bootWhat — תיעוד מלא של הפרויקט

> מסמך זה מתאר את כל מה שנבנה, איך זה עובד, ואיפה כל דבר יושב.
> נכתב כדי שניתן יהיה לתת אותו לבינה מלאכותית שתסביר כל נושא לעומק.

---

## 1. מה זה bootWhat?

bootWhat הוא **פלטפורמת SaaS לניהול בוט WhatsApp** — כלי שמאפשר לבעל עסק לבנות בוט אוטומטי שמגיב ללקוחות ב-WhatsApp, ללא ידע בתכנות.

הפלטפורמה כוללת:
- **בוט WhatsApp פעיל** — עונה לכל הודעה שמגיעה
- **לוח ניהול (Dashboard)** — ממשק ויזואלי לשליטה על הבוט
- **בניית תהליכי שיחה** — ניתוב חכם עם כפתורי בחירה

---

## 2. הטכנולוגיות — מה זה כל דבר ולמה בחרנו בו

### Next.js (גרסה 15)
**מה זה:** מסגרת פיתוח JavaScript שמאפשרת לבנות גם את הממשק הגרפי וגם את ה-API ב-backend — הכל בפרויקט אחד.

**למה בחרנו בו:** במקום לנהל שני פרויקטים נפרדים (אחד לאתר, אחד לשרת), Next.js מאחד הכל. הוובהוק של WhatsApp, ממשק הניהול, ה-API — הכל קובץ אחד.

**שני חלקים בפרויקט שלנו:**
- `src/app/` — ממשק הניהול (מה שרואים בדפדפן)
- `src/pages/api/` — ה-API הסמוי (מה שמתקשר עם WhatsApp ו-AI)

---

### Vercel
**מה זה:** שירות hosting שמפרסם את הפרויקט לאינטרנט. כמו שכירת שרת, רק אוטומטי לחלוטין.

**למה בחרנו בו:** בכל פעם שמעלים קוד (`vercel deploy --prod`), Vercel לוקח את הפרויקט ומפרסם אותו בלינק קבוע. אין צורך לנהל שרת.

**הכתובת שלנו:** `https://boot-what.vercel.app`

**מה יושב שם:**
- `boot-what.vercel.app/dashboard` — לוח הניהול
- `boot-what.vercel.app/api/webhook` — נקודת הכניסה של WhatsApp

**משתני סביבה ב-Vercel (סודות):**
| שם | מה זה |
|---|---|
| `WHATSAPP_TOKEN` | מפתח הגישה לשליחת הודעות WhatsApp |
| `WHATSAPP_PHONE_NUMBER_ID` | מזהה מספר הטלפון ב-Meta |
| `WEBHOOK_VERIFY_TOKEN` | סיסמה סודית שמוכיחה ל-Meta שהשרת שלנו אמיתי |
| `GROQ_API_KEY` | מפתח הגישה ל-AI (Groq) |

---

### WhatsApp Cloud API (של Meta)
**מה זה:** ה-API הרשמי של Meta לשליחה וקבלה של הודעות WhatsApp. זה לא חיבור לאפליקציה — זה API רשמי שמוקצה לעסקים.

**איך זה עובד:**
1. לקוח שולח הודעה ל-WhatsApp
2. Meta מעביר את ההודעה לשרת שלנו (לכתובת ה-Webhook)
3. השרת עונה עם ה-AI
4. Meta מעביר את התשובה ללקוח

**Webhook** — זו כתובת URL שמספקים ל-Meta, ו-Meta "דופק" בה בכל פעם שמגיעה הודעה. כמו כתובת למשלוח.

**הטוקן ואורך חייו:**
- **טוקן זמני:** נוצר בדף API Setup של Meta. פג תוקף כל 24 שעות. בכל כניסה לדף, נוצר טוקן חדש והישן מבוטל — לכן הבוט "נפל" כל פעם.
- **טוקן קבוע (מה שיצרנו):** נוצר דרך System User ב-Meta Business Manager. לא פג לעולם.

**System User** — משתמש מכונה (לא אדם) ב-Meta שמיועד לאפליקציות אוטומטיות. יצרנו אחד בשם "bootWhat Bot" עם הרשאות Admin שאחראי על שליחת ההודעות.

---

### Groq + Llama AI
**מה זה Groq:** חברת AI שמציעה גישה למודלים שפתיים (AI) מהירים מאוד דרך API.

**מה זה Llama:** מודל AI שפתי של Meta (כן, אותה Meta). הוא מסוגל להבין ולהשיב בעברית ובשפות רבות.

**המודל שבחרנו:** `llama-3.3-70b-versatile` — מודל גדול ואיכותי (70 מיליארד פרמטרים).
**מודל גיבוי:** `llama-3.1-8b-instant` — מודל קטן ומהיר, מופעל אוטומטית אם המודל הראשי עמוס.

**Rate Limit** — Groq מגביל כמות פניות לדקה. יש מנגנון אוטומטי: אם המודל הראשי עמוס — עובר למודל הגיבוי.

---

### React Flow
**מה זה:** ספריית JavaScript לבניית תרשימי זרימה ויזואליים עם גרירה וחיבורים.

**למה השתמשנו בו:** לדף "תהליכי שיחה" (Flow Builder) — שם בונים את מסלול השיחה עם צמתים וחיצים.

---

## 3. מבנה הקבצים — מה כל קובץ עושה

```
bootWhat/
├── src/
│   ├── app/                          ← ממשק הניהול (מה שרואים בדפדפן)
│   │   ├── dashboard/
│   │   │   ├── page.tsx              ← דף הסקירה הכללית
│   │   │   ├── ai/page.tsx           ← הגדרות AI (מודל, אופי, הוראות)
│   │   │   ├── flows/page.tsx        ← בונה תהליכי שיחה ויזואלי
│   │   │   ├── auto-replies/page.tsx ← מענה לפי מילות מפתח
│   │   │   ├── broadcasts/page.tsx   ← שליחה המונית
│   │   │   ├── inbox/page.tsx        ← תיבת הודעות נכנסות
│   │   │   ├── templates/page.tsx    ← תבניות הודעה
│   │   │   ├── analytics/page.tsx    ← סטטיסטיקות
│   │   │   └── settings/page.tsx     ← הגדרות מערכת
│   │   ├── globals.css               ← עיצוב כללי (ערכת נושא בהירה, RTL)
│   │   └── layout.tsx                ← מסגרת כללית (HTML lang="he")
│   │
│   ├── pages/api/                    ← ה-API (שרת, לא גלוי למשתמש)
│   │   ├── webhook.ts                ← CRITICAL: מקבל הודעות מ-Meta
│   │   ├── flows.ts                  ← CRUD לתהליכי שיחה
│   │   └── test-ai.ts                ← בדיקת AI מהדשבורד
│   │
│   ├── lib/                          ← לוגיקה עסקית
│   │   ├── groq.ts                   ← שליחת שאלות ל-AI וקבלת תשובות
│   │   ├── whatsapp.ts               ← שליחת הודעות WhatsApp (טקסט + כפתורים)
│   │   ├── config.ts                 ← הגדרות ברירת מחדל של הבוט
│   │   ├── flow-engine.ts            ← מנוע הפעלת תהליכי שיחה
│   │   ├── flow-store.ts             ← אחסון תהליכים בזיכרון השרת
│   │   └── flow-types.ts             ← הגדרות TypeScript לתהליכים
│   │
│   └── components/
│       └── Sidebar.tsx               ← תפריט הניווט הצדדי
│
├── .env                              ← סודות לפיתוח מקומי (לא ב-Git!)
└── vercel.json / next.config.ts      ← הגדרות פריסה
```

---

## 4. איך הבוט עובד — מסלול הודעה מלא

```
לקוח כותב "שלום" ב-WhatsApp
         ↓
Meta מזהה שיש הודעה
         ↓
Meta שולח POST request לכתובת:
https://boot-what.vercel.app/api/webhook
         ↓
webhook.ts מקבל את ההודעה
         ↓
בדיקת כפילות (Deduplication)
[Meta יכולה לשלוח אותה הודעה 2-3 פעמים — מניעת תגובה כפולה]
         ↓
האם ההודעה כבר טופלה? → כן → דלג
         ↓ לא
האם לחץ על כפתור? → כן → flow-engine.ts (ניתוב לפי כפתור)
         ↓ לא
האם יש תהליך שיחה פעיל עם מילת הפעלה תואמת? 
→ כן → flow-engine.ts (הפעל תהליך)
→ לא → getAIReply() ב-groq.ts
         ↓
AI מייצר תשובה
         ↓
sendWhatsAppMessage() ב-whatsapp.ts
         ↓
Meta מעביר את התשובה ללקוח
         ↓
לקוח מקבל תשובה
```

---

## 5. תהליכי שיחה (Flow Builder) — איך עובד

הפיצ'ר החדש שנבנה מאפשר לבנות "מפות שיחה" ויזואליות.

**סוגי צמתים:**
| צומת | מה עושה |
|---|---|
| ⚡ התחלה | מופעל כשלקוח שולח מילת הפעלה (לדוג': "שלום") |
| 💬 הודעה | שולח הודעת טקסט ועובר אוטומטית הלאה |
| 🔘 כפתורי בחירה | שולח הודעה עם עד 3 כפתורים — כל כפתור מוביל לצומת אחר |
| 🤖 AI | הבוט עונה בחופשיות עם AI ועובר הלאה |
| 🏁 סיום | מסיים את השיחה (אופציונלי: שולח הודעת פרידה) |

**כיצד ניתוב הכפתורים עובד טכנית:**
- כל כפתור מקבל ID ייחודי (לדוג': `btn_a`)
- כשלקוח לוחץ על כפתור, WhatsApp שולח `interactive.button_reply.id = "btn_a"`
- המנוע מחפש חיבור (edge) שיוצא מהצומת הנוכחי עם `sourceHandle === "btn_a"`
- מוצא את הצומת הבא ומבצע אותו

**מגבלת WhatsApp:** מקסימום 3 כפתורים בהודעה אחת (הגבלה של Meta).

**אחסון תהליכים:** 
- **בדפדפן:** נשמר ב-localStorage
- **בשרת:** נשמר בזיכרון ה-Lambda של Vercel (זמני — נמחק כשהשרת מתאפס)
- **⚠️ בעיה ידועה:** בפרודקשן, השרת יכול להתאפס ולאבד את התהליכים. לפתרון קבוע נדרש מסד נתונים.

---

## 6. איפה יושב הדשבורד — ⚠️ נושא אבטחה קריטי

### כתובת הדשבורד
`https://boot-what.vercel.app/dashboard`

### האם הוא מוגן?
**לא. הדשבורד פתוח לכל אחד שיודע את הכתובת.**

זה אומר שכרגע:
- כל אחד שיכתוב `boot-what.vercel.app/dashboard` יכול לראות את כל המידע
- כל אחד יכול לשנות הגדרות, ליצור תהליכים, לשלוח הודעות המוניות
- אין שם שאלת סיסמה, אין התחברות, אין הגנה

### למה לא הוספנו הגנה?
כי זה פרויקט MVP ראשוני ושמנו דגש על הפונקציונליות. אבטחה היא הצעד הבא הנדרש.

### מה הפתרון?
יש שתי אפשרויות:

**אפשרות 1 — מהירה (שעה עבודה):**
הגנת סיסמה פשוטה דרך Vercel (Basic Auth) — מוסיפים קובץ `vercel.json` שמחייב שם משתמש וסיסמה לפני כניסה לדשבורד.

**אפשרות 2 — מקצועית (יום עבודה):**
מערכת התחברות מלאה עם NextAuth.js — כניסה עם Gmail/Facebook או שם משתמש וסיסמה. זה הסטנדרט לפלטפורמות SaaS.

---

## 7. מה עובד ומה לא (מצב נוכחי)

### עובד ✅
- בוט WhatsApp פעיל שעונה עם AI
- טוקן קבוע (לא פג תוקף)
- מניעת הודעות כפולות
- בנאי תהליכי שיחה ויזואלי
- כפתורי בחירה אינטראקטיביים
- מענה אוטומטי לפי מילות מפתח
- שליחה המונית (ממשק)
- דשבורד בעברית עם עיצוב בהיר

### לא עובד / דורש עבודה נוספת ⚠️
| בעיה | פתרון |
|---|---|
| הדשבורד פתוח לכל | הוסף התחברות (NextAuth.js) |
| תהליכי שיחה נמחקים כשהשרת מתאפס | הוסף מסד נתונים (PostgreSQL / PlanetScale) |
| תיבת ההודעות (Inbox) ריקה | מחייב מסד נתונים לשמירת שיחות |
| הגדרות ה-AI לא נשמרות | מחייב מסד נתונים |
| שליחה המונית היא ממשק בלבד | צריך חיבור לרשימת אנשי קשר |
| סטטיסטיקות הן נתוני דמו | מחייב מסד נתונים עם לוגים |

---

## 8. מפת הגדרות ה-Meta

**מה יש לנו ב-Meta:**
- **אפליקציה:** "My Bot" (App ID: 993123053726313)
- **WhatsApp Business Account (WABA):** Test WhatsApp Business Account
- **מספר טלפון לבדיקות:** מספר טסט של Meta (לא מספר אמיתי)
- **System User:** bootWhat Bot (ID: 61590662084455) — Admin
- **Webhook:** boot-what.vercel.app/api/webhook (מאומת ופעיל)

**מה ה-Webhook Verify Token:**
`m9WRq2zTQlbr4deJ5ogcpj7uFIVXOtxy` — קוד סודי שנוצר אקראית. Meta שולח אותו בבדיקת האימות, השרת מחזיר אותו, וכך Meta יודע שהשרת שלנו אמיתי.

---

## 9. תהליך פיתוח ועדכון (למתחילים)

### כדי לשנות משהו בקוד:
1. ערוך קובץ ב-VS Code
2. הרץ בטרמינל: `vercel deploy --prod`
3. Vercel בונה ומפרסם תוך ~30 שניות

### כדי לבדוק לוגים (מה קורה בשרת):
```
vercel logs boot-what.vercel.app --json
```

### כדי לעדכן משתנה סביבה:
```
vercel env rm VARIABLE_NAME production --yes
echo "NEW_VALUE" | vercel env add VARIABLE_NAME production
vercel deploy --prod
```

---

## 10. מה הצעדים הבאים המומלצים

לפי סדר עדיפויות:

1. **🔒 הגנה על הדשבורד** (דחוף) — כרגע כל אחד יכול לגשת אליו
2. **💾 מסד נתונים** — לשמירת שיחות, תהליכים, הגדרות
3. **📱 מספר WhatsApp אמיתי** — כרגע על מספר טסט של Meta, לא ניתן לשלוח ליותר מ-5 אנשי קשר מאושרים
4. **🔄 Multi-tenant** — כדי למכור לעסקים אחרים, כל עסק צריך חשבון נפרד
5. **📊 Analytics אמיתי** — חיבור לוגים למסד נתונים

---

## 11. מילון מושגים

| מושג | הסבר |
|---|---|
| **API** | ממשק תקשורת בין תוכנות. כמו שפה משותפת בין WhatsApp לשרת שלנו |
| **Webhook** | כתובת URL שמקבלת הודעות נכנסות אוטומטית |
| **Token / Access Token** | מפתח דיגיטלי שמוכיח זהות. כמו סיסמה שמאפשרת לאפליקציה לפעול |
| **System User** | משתמש מכונה (לא אדם) ב-Meta — עבור אוטומציות |
| **Serverless** | שרת שמופעל רק כשיש בקשה, ונכבה אחרי. Vercel עובד ככה |
| **Lambda** | יחידת קוד שרץ ב-Serverless |
| **Rate Limit** | הגבלת כמות פניות לשרת בפרק זמן |
| **BOM** | תו בלתי נראה שWindows מוסיף לתחילת קבצים/טקסטים. גרם לבאג ב-API Key |
| **RTL** | Right To Left — כיוון כתיבה מימין לשמאל (עברית) |
| **Flow Builder** | הכלי הויזואלי לבניית תהליכי שיחה |
| **Edge** | חיבור (חץ) בין שני צמתים בתרשים הזרימה |
| **Node** | צומת (תיבה) בתרשים הזרימה |
| **WABA** | WhatsApp Business Account |
| **Next.js App Router** | דרך חדשה לנהל נתיבים ב-Next.js (ממשק הניהול) |
| **Next.js Pages Router** | דרך ישנה ויציבה יותר (ה-API שלנו, בגלל באג בגרסה החדשה) |
| **Deduplication** | מניעת עיבוד כפול של אותה הודעה (Meta שולחת לפעמים כפולים) |

---

*תיעוד זה נוצר ב-18.06.2026*
*גרסת Next.js: 15.5.19 | גרסת @xyflow/react: עדכנית | מודל AI: llama-3.3-70b-versatile*
